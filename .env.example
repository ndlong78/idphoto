# Optional runtime config injected via window.__IDPHOTO_CONFIG__
# Production: use HTTPS endpoint only.
# Local dev: HTTP is allowed only for localhost / 127.0.0.1 / [::1].
TELEMETRY_ENDPOINT=https://your-analytics.example.com/events
TELEMETRY_CONSOLE_LEVEL=error
